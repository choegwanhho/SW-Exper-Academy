#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int N = 0;
    cin >> N;

    vector<int> nums(N);
    for (int i = 0; i < N; i++)
        cin >> nums[i];

    nth_element(nums.begin(), nums.begin() + N / 2, nums.end());

    cout << nums[N / 2] << "\n";

    return 0;
}
